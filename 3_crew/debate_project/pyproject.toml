[project]
name = "debate_project"
version = "0.1.0"
description = "debate_project using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[tools]>=0.130.0,<1.0.0"
]

[project.scripts]
debate_project = "debate_project.main:run"
run_crew = "debate_project.main:run"
train = "debate_project.main:train"
replay = "debate_project.main:replay"
test = "debate_project.main:test"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "crew"
